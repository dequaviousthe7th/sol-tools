name = "solreclaimer-api"
main = "src/index.ts"
compatibility_date = "2024-05-12"

# After creating the KV namespace with:
#   npx wrangler kv:namespace create STATS
# Replace the id below with the actual KV namespace ID
[[kv_namespaces]]
binding = "STATS"
id = "4275743e438c4f91a34e6ad09225ffaf"

# Custom domain: api.solreclaimer.net
# Set up via Cloudflare dashboard after initial deploy

[vars]
ENVIRONMENT = "production"
